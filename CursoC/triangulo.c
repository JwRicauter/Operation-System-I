#include<stdio.h>

int main(){
	// Ejercicio area de un triangulo.

	float base, altura, area;
	base = 5.25;
	altura = 17.32;
	area = (base * altura) / 2;
	printf("Base: %f Altura: %f Area: %F \n", base, altura, area);


	return 0;
}